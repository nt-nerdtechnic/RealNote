[project]
name = "meeting-minutes-asr"
version = "0.1.0"
description = "Local CLI pipeline for Breeze-ASR meeting transcription and meeting minutes generation."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
